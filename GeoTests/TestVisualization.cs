using Geo;

namespace GeoTests;

/// <summary>
/// No-op in camber. CSG's test viewer is not part of this tree.
/// </summary>
public static class TestVisualization
{
    public static void Visualize(GeoAPI api, string testName, bool visualizeSketches = true)
    {
        _ = api;
        _ = testName;
        _ = visualizeSketches;
    }
}
